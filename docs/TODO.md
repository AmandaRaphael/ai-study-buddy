# TODO

- Make the `Recent` list behave like true recent history by moving a chat to the top when it receives a new message, not only when it is first created.
- Decide whether a "new chat page" should remain an in-app state switch or become a real routed page with URL/back-button/refresh support.
- Either use `createdAt` for visible sorting or metadata, or remove it if chat ordering no longer depends on it.
